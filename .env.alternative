# Alternative: Separate database credentials
DB_HOST="72.60.194.28"
DB_PORT="3306"
DB_NAME="webbaharihook"
DB_USER="Bahari"
DB_PASSWORD='dWs%3X>MD^(WHv]0<0.5B`t256@"mg'

# Construct URL in application (tidak di .env)
# DATABASE_URL akan dibuat di aplikasi dengan:
# mysql://${DB_USER}:${encodeURIComponent(DB_PASSWORD)}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# NextAuth
NEXTAUTH_SECRET="your-super-secret-nextauth-secret-key-here"
NEXTAUTH_URL="https://webbaharihook2.baharihari.com"

DOMAIN="webbaharihook2.baharihari.com"